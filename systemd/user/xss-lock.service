[Unit]
Description=Lock the screen on idle and sleep
PartOf=graphical-session.target
After=graphical-session.target

[Service]
ExecStart=/usr/bin/xss-lock --transfer-sleep-lock -- %h/.config/i3/scripts/lock
Restart=on-failure
RestartSec=1
Slice=session.slice
